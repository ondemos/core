import generateIdentities from "./generateIdentities";
import generateCommitDetails from "./generateCommitDetails";
import generateProof from "./generateProof";
import commit from "./commit";
import verifyProof from "./verifyProof";
import memory from "./memory";

export default {
  generateIdentities,
  generateCommitDetails,
  generateProof,
  commit,
  verifyProof,
  memory,
};
